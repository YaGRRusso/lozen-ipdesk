import { DomainTS } from "../types/domainType";

export const sectionsApi = {
   getSections: async (zd: DomainTS) => {

   },
   createSection: async (zd: DomainTS) => {

   },
   deleteSection: async (zd: DomainTS) => {

   }
}