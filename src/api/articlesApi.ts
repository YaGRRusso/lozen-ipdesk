import { DomainTS } from "../types/domainType";

export const articlesApi = {
   getArticles: async (zd: DomainTS) => {

   },
   createArticle: async (zd: DomainTS) => {

   },
   deleteArticle: async (zd: DomainTS) => {

   }
}