import { DomainTS } from "../types/types"

const zendesk: DomainTS = {
   subdomain: 'con-aktienow',
   email_address: 'ps@aktienow.com',
   password: '#ps@2020',
   locale: "pt-br"
}

export default zendesk